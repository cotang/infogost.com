- var application = [{name: 'application', title : 'Заказать документ онлайн', button: 'Заказать документ', success: 'Ваша заявка отправлена'}] 
- var applicationHeader = [{name: 'application-header', title : 'Рассчитать стоимость', text: 'Узнать сроки и стоимость услуги онлайн', button: 'Узнать стоимость', success: 'Ваша заявка отправлена'}] 
- var question = [{name: 'question', title : 'Задать вопрос', button: 'Задать вопрос', success: 'Ваш вопрос отправлен'}] 
- var call = [{name: 'call', title : 'Для получения бесплатной консультации заполните форму', button: 'Отправить', success: 'Ваш запрос отправлен'}] 


mixin form(items)
  each item in items
    +b.SECTION(class= 'form--' + item.name)
      form(action='', method='')
        //- +e.A.close(href='#')
        +e.H3.title #{item.title}          

        if item.name == 'application-header'
          +e.P.text #{item.text}
          .form-group.field-product.required
            +e.INPUT.input(type='text', name='Продукт', placeholder="Наименование продукта*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-name.required
            +e.INPUT.input(type='text', name='Имя', placeholder="Ваше имя*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-tel.required
            +e.INPUT.input(type='tel', name='Телефон', placeholder="Ваш телефон*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-email.required
            +e.INPUT.input(type='email', name='Email', placeholder="Ваш email*")
            p.help-block Ошибка заполнения поля формы

        if item.name == 'application'
          .form-group.field-product.required
            +e.INPUT.input(type='text', name='Продукт', placeholder="Наименование продукта*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-name.required
            +e.INPUT.input(type='text', name='Имя', placeholder="Ваше имя*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-tel.required
            +e.INPUT.input(type='tel', name='Телефон', placeholder="Ваш телефон*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-email.required
            +e.INPUT.input(type='email', name='Email', placeholder="Ваш email*")
            p.help-block Ошибка заполнения поля формы

        if item.name == 'question'
          .form-group.field-name.required
            +e.INPUT.input(type='text', name='Имя', placeholder="Ваше имя*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-email.required
            +e.INPUT.input(type='email', name='Email', placeholder="Ваш email*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-tel
            +e.INPUT.input(type='tel', name='Телефон', placeholder="Ваш телефон")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-question.required
            +e.TEXTAREA.textarea(name='Вопрос', placeholder="Ваш вопрос*") 
            p.help-block Ошибка заполнения поля формы

        if item.name == 'call'
          .form-group.field-name.required
            +e.INPUT.input(type='text', name='Имя', placeholder="Ваше имя*")
            p.help-block Ошибка заполнения поля формы
          .form-group.field-tel.required
            +e.INPUT.input(type='tel', name='Телефон', placeholder="Ваш телефон*")
            p.help-block Ошибка заполнения поля формы

        //- if item.name == 'review'
        //-   .form-group.field-name.required
        //-     +e.INPUT.input(type='text', name='Компания', placeholder="Ваша компания*")
        //-     p.help-block Ошибка заполнения поля формы
        //-   .form-group.field-email.required
        //-     +e.INPUT.input(type='email', name='Email', placeholder="Ваш e-mail*")
        //-     p.help-block Ошибка заполнения поля формы
        //-   .form-group.field-review.required
        //-     +e.TEXTAREA.textarea(name='Отзыв', placeholder="Ваш отзыв*") 
        //-     p.help-block Ошибка заполнения поля формы

        //- .form-group.field-country.required
        //-   +e.SELECT.country-list
        //-     option Россия
        //-     option Беларусь
        //-     option Казахстан
        //-     option Украина
        //-     option Азербайджан
        //-     option Армения    
        //-     option Грузия
        //-     option Молдавия

        //- .form-group.field-region.required
        //-   +e.SELECT.region-list
        //-     option Санкт-Петербург и Ленинградская область
        //-     option Москва и Московская область
        //-     option Новгородская область
        //-     option Псковская область
        //-     option Республика Карелия
        //-     option Мурманская область
        //-     option Калининградская область
        //-     option Вологодская область
        //-     option Тверская область

        .form-group
          if item.name == 'application-header'
            +b.BUTTON.btn--form.--header(type='submit') #{item.button}
          else
            +b.BUTTON.btn--form(type='submit') #{item.button}
          p.help-block.help-block-success #{item.success}
